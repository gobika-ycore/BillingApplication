// Firestore Security Rules for Billing Application
// These rules control access to your Firestore database

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // TEMPORARY: Allow all access for testing (CHANGE THIS AFTER TESTING!)
    // This is for development only - deploy proper auth rules for production
    match /{document=**} {
      allow read, write: if true;
    }
    
    // PROPER RULES (use these after confirming anonymous auth works):
    // match /{document=**} {
    //   allow read, write: if request.auth != null;
    // }
    
    // PRODUCTION RULES (uncomment and modify when ready for production)
    /*
    // Users collection - only authenticated users can access their own data
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Customers collection - authenticated users only
    match /customers/{customerId} {
      allow read, write: if request.auth != null;
      allow create: if request.auth != null && 
        request.resource.data.keys().hasAll(['name', 'acNo', 'phone']) &&
        request.resource.data.name is string &&
        request.resource.data.acNo is string &&
        request.resource.data.phone is string;
    }
    
    // Sales Bills collection - authenticated users only
    match /salesBills/{billId} {
      allow read, write: if request.auth != null;
      allow create: if request.auth != null && 
        request.resource.data.keys().hasAll(['billNo', 'customerName', 'amount']) &&
        request.resource.data.billNo is string &&
        request.resource.data.customerName is string &&
        request.resource.data.amount is number &&
        request.resource.data.amount > 0;
    }
    
    // Collection Bills collection - authenticated users only
    match /collectionBills/{billId} {
      allow read, write: if request.auth != null;
      allow create: if request.auth != null && 
        request.resource.data.keys().hasAll(['billNo', 'customerName', 'collectionAmount']) &&
        request.resource.data.billNo is string &&
        request.resource.data.customerName is string &&
        request.resource.data.collectionAmount is number &&
        request.resource.data.collectionAmount > 0;
    }
    
    // Products collection - authenticated users only
    match /products/{productId} {
      allow read, write: if request.auth != null;
    }
    
    // Settings collection - authenticated users only
    match /settings/{settingId} {
      allow read, write: if request.auth != null;
    }
    */
  }
}
